import {StyleSheet} from "react-native"

const CommonStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F5FCFF',
  },
  inputWithVerticleMargin : {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
})

export default CommonStyles;